// Author: Green Onyeji (https://github.com/ositano/) (https://www.linkedin.com/in/green-onyeji-11608a97/)
// lib/core/enums/enums.dart

enum Flavor { debug, release, profile }

enum MethodType { post, get, put, delete, patch }

enum RequestStatus { initial, loading, loaded, error }

enum RouteType { named, path }

enum RouteLevel { normal, top, replace }

enum MessageType { success, error, info, warning }
